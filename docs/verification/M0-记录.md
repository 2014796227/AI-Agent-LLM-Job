# M0 事实核验记录（verification）

> 规范（provenance.md）：只记录真实执行的命令与真实输出；失败与修复过程原文保留；事实参数以实测为准回填蓝图/ADR。
> 状态标记：✅已完成 ｜ ⏳待执行（标明阻塞条件）｜ ❌失败（附修复）

## 1. 环境检查 ✅（2026-08-15，本机 Windows/Git Bash）

命令：`python --version; node --version; git --version; docker --version; curl -sI <目标>`

| 项 | 结果 |
|---|---|
| Python（默认） | 3.14.2（**项目统一 3.11**：本机装有 3.11.0，`py -3.11` 可用） |
| pip | 25.3 |
| Node / npm | v24.12.0 / 11.6.2 |
| git | 2.55.0.windows.3 |
| Docker | 29.6.2（Docker Desktop） |
| open.bigmodel.cn 连通 | HTTP 200 |
| akshare.akfamily.xyz 连通 | HTTP 200 |
| pypi.tuna.tsinghua.edu.cn 连通 | HTTP 200 |

## 2. 事实核验（来自公开资料检索，2026-08-15）✅

| 事实 | 结论 | 来源 |
|---|---|---|
| 智谱 embedding 模型名 | `embedding-3`（默认 2048 维，dimensions 可选 256/512/1024/2048）；`embedding-2` 固定 1024 维 | docs.bigmodel.cn 官方文档 |
| embedding-3 默认维度 vs pgvector HNSW | 默认 2048 > 索引上限 2000 ⇒ 必须显式 dimensions=1024 | pgvector 文档 + 官方文档交叉 |
| dimensions 与返回维度偶发不匹配 | 社区有真实反馈 ⇒ 启动探针+双侧断言+embedding-2 回退（已入蓝图 rag.py） | GitHub issue |
| AKShare 现状 | 免费可用、持续维护；底层爬公开源、无 SLA、接口偶随数据源改版失效 | akshare 官方文档（实测装得 1.18.91） |

## 3. GLM API 真实调用 ✅（2026-08-15，用户提供 key；脚本 `scripts/m0_llm_checks.py`）

- [x] **C1** glm-4.6 真实调用：`text='收到' usage_tokens=189`（复跑 167）；glm-4.7-flash：`usage_tokens=154`（复跑 146/170）
- [x] **zhipuai SDK `tools=None`（v18 遗留项）**：生产 `llm.chat()` 默认即传 `tools=None`（supervisor/critic/writer 路径）——上述 C1 全部成功即证明 SDK 序列化可接受、API 正常应答，无需"None 时不传"修复
- [x] 流式 tool_calls 增量拼接：flash 流式返回逐块拼出 `name=get_weather args='{"city":"北京"}'`（合法 JSON）
- [x] flash 真实工具往返（agent_loop 生产 schema）：`name=market.price_history args={"symbol":"600519","start":"20240102","end":"20240115"}`
- [ ] **C2** embedding-3(dimensions=1024)：**阻塞**——`429 code 1113 余额不足或无可用资源包`（见下 key 差异）；DDL `vector(1024)` 依据暂维持 M0 §2 文档事实
- [ ] embedding-2 不带 dimensions（v17 P1-3 验证）/ 带 dimensions（回填 ADR-0005）：同被 1113 阻塞，待按量余额
- [x] **coding-plan key 与按量 key 行为差异（本 key 实测结论）**：
  - glm-4.7-flash（免费层）：**稳定可用**
  - glm-4.6（付费侧）：**间歇性 1113**（两次成功后开始报，60s 后 glm-4.6 一度恢复又复现——非 RPM 限流，是余额/资源包口径的配额约束；生产 `chat()` 的 3 次重试 + 4.6→flash fallback 链实测可吸收，端到端任务据此完成）
  - embedding-3 / embedding-2：**持续 1113**（疑似不在该 key 资源包范围内，仅按量计费）
  - 结论：**正式演示/生产部署需按量付费 key 或充值**，否则 RAG 向量检索永久 BM25 降级、4.6 依赖间歇回退 flash（质量口径偏离 ADR-002）

## 3.5 端到端真实任务（三跑两修复，末跑全绿）✅

脚本 `scripts/m0_e2e_task.py`（输入：分析 600519 近三年走势+回测双均线，区间 2023-06-01~2026-05-31；行情走容器内缓存——东财容器出网被拒的既定环境边界）：

- **首跑**（trace 508efb706492）：Supervisor 规划成功→**首个 Agent 节点即 `ImportError: cannot import name 'registry' from 'app.tools'`**——v18 起蓝图缺陷（`agent_loop` 引用不存在的对象；单测不覆盖 `run_agent`、两轮静态审查漏检）→ 修复（v20）
- **二跑**（trace 51b0e3e30333）：管线走通至 task_done，但行情窗口被解析为 **2021-2024**（模型无当前日期概念，训练截止时钟）→ 缓存 miss→数据失败→**报告如实声明"数据获取失败"且零编造数字**（诚实性规范实证）；SSE 经 nginx 收到 20 事件无缓冲（预判#3 ✅）→ 日期锚点修复（v20）
- **末跑**（trace **4fd2ac796bd7**）：**全绿**——research→strategy→writer→critic→done，41 事件/210s；3 个 artifact 落库；报告 1488 字符、全部数字带 `[[art_id]]` 引用：区间跌幅 -8.93%（hfq 口径）、年化波动 19.76%、最大回撤 -28.40%、回测总收益 -18.74%/夏普 -0.64/净值 0.8126（与 artifact 一致）；raw 展示价与 hfq 计算口径的标注纪律正确（茅台分红致 raw 价差 ≠ hfq 收益，报告区分两者）；critic 一轮 pass
- **连带安全发现**：重建镜像时确认 `COPY . .` 把含 key 的 `backend/.env` 烤进镜像 → 新增 `backend/.dockerignore`（v20），重建后验证 `/app/.env` 不存在、运行期密钥仍由 compose env_file 注入
- **环境留档**：宿主 diskcache 目录直拷进容器卷不可靠（`key in cache` True 而 `get()` None——索引/blob 跨环境拷贝非受支持路径）；改为容器内 diskcache API 从 fixture 重灌（724 行命中验证通过）

## 3.6 免费运行模式（2026-08-15，用户决定"API 转免费"）

- 配置：`backend/.env` 以 env 覆盖 `PLANNER_MODEL=glm-4.7-flash`、`JUDGE_MODEL=glm-4.7-flash`（worker/fallback 本即 flash）——四角色全部落在该 key 的免费层；**代码与 ADR-002 设计分工不变**（按量余额恢复后删除两行 env 即回标准形态）；embedding 探针失败→检索 BM25 降级（设计内路径）
- 端到端验证：报告 1766 字符、数字正确、节点引用齐全；首跑终态 `degraded: budget:wall_clock`（免费层 QPM 退避叠加长报告生成触碰默认 300s 墙钟——**预算降级路径真实工作**，产出带标注的部分结果符合 PRD 非功能口径）；处置=`BUDGET_WALL_CLOCK_S=600`（免费运行期 env 上调，复跑结果见下方追加行）
- 边界如实声明：flash 单模型链无跨模型回退（v17 D-1 既定取舍），免费层限流波动下个别调用靠 3 次重试吸收；报告质量口径低于 4.6 分工（ADR-002 预期），演示可接受
- 复跑结果（墙钟 600s 后）：管线推进至 critic 修订循环，最终 **failed：429 code 1305「该模型当前访问量过大」**（免费层过载限流，区别于 1113 余额）——原 3 次×1s/2s 退避不足以吸收秒级过载突发 → **v21 修复**：chat 重试升 4 次、退避 2s/4s/8s（蓝图同步）；免费模式完整画像：任务时长 6~11 分钟（限流退避拉长）、过载窗口可能失败、数据/报告质量本身达标（前一次 1766 字符全数字正确）

## 3.7 embedding 三层回退链与向量检索恢复（2026-08-15，用户提供 SiliconFlow key，蓝图 v22）

- **key 实测**（key 存 backend/.env，gitignore+dockerignore 双保护）：`GET /v1/models` 正常（BAAI/bge-m3、bge-large-zh-v1.5 等 8 个 embedding 模型）；`POST /v1/embeddings` bge-m3 返回 **1024 维**——与 DDL `vector(1024)` 精确匹配
- **实现**（v22）：config 增 siliconflow 三字段（key 为空自动跳过该层）；`llm.embed()` 按 `EMBED_PROVIDER` 分发（httpx+Bearer，zhipuai SDK 的 JWT 签名不适用第三方）；`rag.probe()` 链 [zhipu e3 → zhipu e2 → siliconflow bge-m3]
- **验证链**：宿主探针通过（三层日志：e3 1113→e2 1113→**bge-m3 1024 OK**）；29 单测绿；容器重建后 `/api/healthz` → **`{"ok":true,"vector_ok":true}`**（M0 以来首次向量层可用）；`migrations/002_hnsw.sql` 应用成功（CREATE INDEX）；容器内向量检索 drill（`scripts/m0_drill_vector.py`）：真实 bge-m3 嵌入入库 → `mode=vector` 近邻命中（相似度 0.7286）
- **C2 状态更新**：智谱 embedding-3 维度实测仍待按量余额（1113）；**向量链路可用性已由 bge-m3 等价验证**（同 1024 维契约、探针+检索+HNSW 全链路实证）——C2 的工程目的（DDL 维度定稿依据）已达成，智谱侧数值留待余额后补录
- **边界**：embedding 侧自此非单厂商（LLM chat 仍纯 GLM，ADR-002 不变；ADR-0005 已补注）；corpus 与 query 必须同一模型编码（probe 全局设定，天然满足）

## 4. AKShare 数据实证 ✅（2026-08-15，脚本 `scripts/m0_akshare_checks.py`，Py3.11 venv）

- [x] **B1** 600519 hfq+raw 双口径拉取 + schema 校验：724 行 × 10 列（open/high/low/close/volume × hfq/raw，date 索引），区间 2023-06-01~2026-05-29；`fetch_combined` 内置中文列校验通过
- [x] **B2 hfq 重叠窗口实证**：窗口 2023-06-01~2024-06-30 与 2023-06-01~2026-05-31，重叠 261 个交易日，**10 列逐日最大绝对差全部 = 0.0 → 一致**（ADR-0003 已回填："该数据源上 hfq 历史不随窗口末端变化的性质成立"）
- [x] **B3 冻结快照 ×2**：`evals/fixtures/600519_hfq_20230601_20260531.parquet`（724 行，sha1 236554f43075…）与 `600519_hfq_20240101_20260531.parquet`（580 行，sha1 9f278689950b…）；meta sidecar（fetched_at UTC/akshare_version=1.18.91/checksum）同名 `.meta.json`；`tools._load_fixture` 回读校验（10 列齐备）通过
- [x] 预判故障 #1（列名改版）：构造缺列表 → `AssertionError: 数据源schema变更: ['日期', '开盘']` 按预期触发

环境留档：pandas 2.2 对 `pd.read_json(literal str)` 发 FutureWarning（缓存命中路径；~=2.2 钉版下功能正常，升级 pandas 3 前需改 StringIO——留档观察，不在本轮修）。

## 5. 代码级演练 ✅（2026-08-15；单测在 backend venv，D 系在 Compose 容器内/宿主机）

### 5.0 落仓与单测

- [x] 蓝图 v18 全部代码落仓为真实文件（backend/frontend/deploy/scripts/evals/migrations，57 文件）
- [x] **A1~A4：`pytest` 29 项全绿**（test_backtest 4 + test_dsl 15 + test_events_replay 6 + test_rag_chunk 3 + 修复后复跑全绿，1.58s）

### 5.1 M0 实测发现并修复（蓝图 v18 → v19；失败原文如下）

| # | 发现 | 级别 | 失败原文（摘） | 修复 |
|---|---|---|---|---|
| 1 | compose 顶键 `volumes:` 缩进在 `services:` 之下 | 部署阻断 | `validating docker-compose.yml: services.volumes additional properties 'appdata', 'marketcache', 'pgdata' not allowed` | 移回顶层 |
| 2 | Dockerfile CMD JSON 数组跨行 | 部署阻断 | `dockerfile parse error on line 11: unknown instruction: "--proxy-headers",` | 合并单行 |
| 3 | pydantic-settings v2 默认 extra='forbid' 拒绝 .env 共享变量 | P0 | `ValidationError: db_pass Extra inputs are not permitted [type=extra_forbidden]`（本地 pytest 收集全挂；容器同因启动即崩） | `SettingsConfigDict(env_file=".env", extra="ignore")` |
| 4 | `httpx~=0.27` 解析到 0.28.1（0.28 移除 sniffio，zhipuai 2.1.5 直接 import sniffio） | P0 | `ModuleNotFoundError: No module named 'sniffio'`（pip 安装不报错、import 期崩溃） | 钉 `httpx~=0.27.0` |
| 5 | test_fill_timing_contract：signal_close 断言与契约矛盾 | 测试缺陷 | `assert 0.10000000000000009 < 1e-09`（实际净值 1.0） | 断言改 1.0/收益 0 |
| 6 | test_hand_computed_with_fee：手算漏计第二笔费 | 测试缺陷 | `assert 0.0004999... < 1e-9`（d4 应为 1.0995×0.9995≈1.0990） | 断言改 1.099 |
| 7 | test_max_drawdown：捕获段净额恰为 1 | 测试缺陷 | `assert 0.0 > 0`（9/12×13/9×12/13=1） | 末值 12→14 |
| 8 | test_hhv_excludes_today：5 行撑不起 MA20 窗口 | 测试缺陷 | `CompileError: 数据仅5行，不足以计算窗口21`（编译期正确拒绝） | 扩至 25 行 |
| 9 | test_chunk_page_attribution：15 字符<20 字符阈值 | 测试缺陷 | `ValueError: 第1页无文本层（扫描件，本期不支持OCR）` | 文本加长 |

判读：#5~#9 均为**测试错、实现对**（对照 docstring 契约与 ADR-0004 推演）——历轮静态审查"数值已手算复核"实为纸面推演，从未执行；这正是 M0 实跑的价值。#1~#4 均已同步修复蓝图与仓库（v19 版本记录留痕）。

### 5.2 D1 Compose 三容器 ✅

`docker compose up -d`（修 #1/#2/#3/#4 后）：db `healthy`、api/web `Up`；`curl 127.0.0.1:8000/api/healthz` → `{"ok":true,"vector_ok":false}`；经 nginx `curl 127.0.0.1/api/healthz` 同；启动日志 `{"interrupted":0,"requeued":0,"vector_ok":false,"event":"boot"}`，探针双模型失败为 warning 不阻止启动。

### 5.3 D2 guarded finish 冲突演练 ✅（`scripts/m0_drill_guarded_finish.py`，api 容器内）

- [1] 心跳停超租约→`watchdog_tick`：`interrupted=1 status=interrupted error=lease_expired usage.reserved 50000 -> 0`（预留释放）
- [2] 迟到的编排器 `_finalize`（同 worker）：`ok=False status=interrupted result=None(未覆盖) M_TASK_CONF 0.0->1.0 usage.reserved=0(未再释放)`；结构化日志 `finish_conflict want=done` 同步留痕
- [3] **重启对账断言（v18 P1-1 回归）**：人为污染 +7777 后 `recover_on_boot`：`对账前 37777 → 对账后 30000 == Σ(pending.reserved)`，requeued=1
- 演练脚本自身踩坑留痕：初版直接 `tasks.create(reserved=…)` 未先 `reserve_daily`（预占在 /api/chat 路径完成）——修正为镜像完整 API 路径后通过

### 5.4 D3 并发冒烟 ✅（HTTP 面 `m0_drill_concurrency_http.py` + 容器内 `m0_drill_concurrency.py`）

- HTTP 面：两任务并行提交 0.031s 返回 task_id；运行期间 healthz 60 次 **max 16.0ms / avg 3.1ms**；两任务均 running→终态 `failed: RuntimeError: LLM 全部重试失败: Connection error.`（无 key 的诚实结果）
- 容器内：两路 to_thread 并发网络 IO 12.1s 共 180 请求，事件循环 1180 个 10ms 采样 **max_lag 2.2ms**；心跳续租 59 次，租约持续有效
- **环境发现留档**：东财接口从 Docker Desktop(Windows) 容器出网被断连（宿主机正常；容器对 baidu/sina 正常）——演练载荷改用新浪源；生产 Linux 主机部署时需复测；不影响缓存/fixture 路径

### 5.5 C3 代码级降级演练 ✅（`scripts/m0_drill_bm25_fallback.py`）

`VECTOR_OK=True`（模拟探针 OK）+ 无 key（embed 必败）→ `mode=bm25_degraded note=向量检索暂不可用，已降级为关键词检索 hits=[('doc_drill_c3',1)] M_RAG_FALLBACK 0.0->1.0`，日志 `rag_vector_fallback_bm25` 留痕。前端标注随真实任务报告验证（待 key）。

### 5.6 D4 备份链演练 ✅（含偏差记录）

- `bash -n deploy/backup.sh` 语法 OK；**Git Bash 无 flock（util-linux）→ 按等价链人工逐步执行**（flock 互斥留服务器首次部署验证，偏差如实记录）
- ① `pg_dump | gzip` → db.sql.gz（2523B，`gunzip -t` 完整性 OK）；② `docker run alpine tar czf` appdata 卷（卷内暂无工件，87B 仅目录项——无 key 任务在 Supervisor 阶段失败未产生工件，符合预期）；③ `POST /api/admin/ttl` → `{"deleted":0}`
- **reconcile 零悬空**：`{"dangling": [], "orphans_removed": 0}`（api 容器内执行，部署手册 §3 步骤 4）
- 安全检查单：外部 `POST /api/admin/ttl` → 403；外部 `GET /metrics` → 403；回环 `:8000/metrics` → 200
- 附带实证：一次演练脚本崩溃遗留的 running 任务被 **uvicorn 自带 watchdog 自动回收**（`interrupted/lease_expired`）——租约自愈链真实生效

### 5.7 E 环境一致性 ✅

- E1：Py3.11.0 venv `pip install -r requirements.txt` 全成功（含 #4 修复后复装；jsonpath 走 setup.py install 的良性 deprecation 提示）
- E2：`npm install`（78 包）+ `npm run build`（`tsc -b` 0 错误；vite 622 模块；chunk>500kB 警告为 echarts 体积，良性）

## 6. 预判故障与验证清单

| # | 预判故障点 | 验证方式 | 结果 |
|---|---|---|---|
| 1 | AKShare 接口列名改版 | 人为改 NEED_COLS 触发校验报错 | ✅ 触发（§4） |
| 2 | embedding 维度不匹配 | 探针注入异常维度→降级路径 | ✅ 探针失败路径实证（无 key 时 Connection error、有 key 时 1113——两种失败均 `vector_ok=false` 不阻止启动）；查询级降级 C3 已实证；维度正常路径待按量余额 |
| 3 | SSE 被 nginx 缓冲 | 本地 nginx 起服务对比开关 | ✅ **实证不缓冲**：端到端任务 SSE 经 nginx 80 端口收满 20/41 事件（§3.5，`proxy_buffering off` 生效） |
| 4 | 事件队列溢出 | 慢消费者剧本（已有单测） | ✅ 单测 test_overflow_notifies_and_closes 绿 |
| 5 | 小内存服务器 npm build OOM | 服务器实测（M2 部署时） | ⏳ 待 M2 |

## 7. 修改记录

- 2026-08-15：建档；第 1、2 节完成（真实数据）。
- 2026-08-15：v17 静态逐行审查（报告：`v17-审查报告.md`）——蓝图 v16→v17（P0×1/P1×4/P2×3/P3×10）；§3 新增 embedding-2 无 dimensions 实测项。
- 2026-08-15：v18 独立逐行复核（报告：`v18-审查报告.md`）——蓝图 v17→v18（P1×1：recover_on_boot 预留双重释放；P2×1：fixture 10 列契约；P3×6+设计确认 D-5+内容钉死×5）；§3 新增 tools=None 实测项；D2 演练扩展断言（重启后 reserved=Σ pending）。
- 2026-08-15：**M0 执行**——蓝图 v18 代码落仓（57 文件）；单测/依赖/部署/演练全量实跑；发现并修复 9 项（§5.1 表），蓝图升 **v19**；B2 hfq 实证=一致（ADR-0003 回填）；fixtures ×2 生成；C1/C2 及 §3 实测项仍阻塞待 API key。
- 2026-08-15：**M0 端到端补验（用户提供 key）**——§3 实测（C1/tools=None/流式/工具往返 ✅；C2 与 embedding-2 观察 1113 阻塞，key 差异结论记录）；§3.5 端到端三跑两修复（agent_loop registry P0 / 日期锚点 P1 / .dockerignore 安全项），末跑全绿（trace 4fd2ac796bd7）；SSE 经 nginx 实证（预判#3 闭环）；蓝图升 **v20**（含 M1 CI workflow 钉死）。
