# 验收报告：M5 评测与加固

- 验收人：本人（证据由 AI 编码助手执行留痕，provenance §二）
- 日期：2026-08-16
- 被测版本：commit 70f5332（蓝图 v28）；线上 http://43.156.248.38
- 验收范围：评测全量（脚本生成制）、备份链演练与每日 cron、限流验证、演示场景定稿（待本人）

## 1. 备份链演练（生产真跑，2026-08-16）

| 步骤 | 结果 | 证据 |
|---|---|---|
| 演练前安全副本 | pg_dump + tar 各一份至 /home/ubuntu/safety | 命令留痕 |
| backup.sh 全链（flock→dump→tar→TTL） | ✅ `backup 2026-08-16 done`；db.sql.gz 1.6MB（gunzip -t 通过）、appdata.tar.gz 2.4MB（含 4 份语料 PDF + 7 工件） | /var/backups/alphadesk/2026-08-16/ |
| dump 恢复验证（临时库 alphadesk_restore） | ✅ 恢复 0 错误；docs/chunks/tasks 计数与生产完全一致（4/369/9） | 对账输出 |
| 卷恢复零悬空 | ✅ **恢复库全部 artifact 路径与 tar 内容逐一对账一致（7/7）** | tar -tzf vs 恢复库 SELECT 对账 |
| 每日 cron | ✅ root crontab `0 4 * * *`（env 文件 /root/alphadesk.env 权限 600） | crontab -l |
| 环境偏差修正 | backup.sh 从 Windows git 检出无执行位 → 服务器 chmod +x（首次运行 `sudo bash` 兜底）；演练脚本两处自查错误已修正（PG 不支持跨库查询须 `-d` 连接；悬空校验须用恢复库路径而非实时库路径） | 本报告 §3 |

## 2. 限流验证（2026-08-16）

容器内 `ratelimit.allow()` 21 次：前 20 次 True、**第 21 次 False（按设计拒绝）**、不同 IP 不受影响（True）。生产路径的 429 行为已由此函数级验证覆盖（HTTP 层共用同一实现）。

## 3. 问题清单

| # | 描述 | 严重级 | 状态 | Issue |
|---|---|---|---|---|
| 1 | backup.sh 无执行位（Windows git 检出丢 +x）——首次运行报 command not found | P3 | 已处置（服务器 chmod +x；正式根治可加 .gitattributes `*.sh text eol=lf`） | 待建 |
| 2 | 评测进程以 `sh -c nohup` 包装经 `compose exec -d` 启动后被静默终止（无日志无 traceback） | P3 | 已规避：直接 `exec -d python -u` 启动稳定运行 | 待建 |
| 3 | 东财跨 IP 累计限流持续（服务器+家宽双 IP 复发，阈值极低） | P2 | 演示依赖预灌缓存；评测回测/报告类全部走 fixture（确定性）；换源列 P2 | 待建 |

## 4. 评测全量（进行中）

- 评测集候选 45 条（回测 12/报告 8/RAG 20/正确拒绝 5，`evals/cases/*.yaml`，AI 生成**待本人删改定稿** per 评测说明 §1）；harness 增强：`must_cite` 断言 + 报告 cite 列（v28）
- **smoke（4 条子集，2026-08-16 03:15 UTC）**：harness 全链验证通过——backtest_001 四项确定性断言全真（tools/spec/backtest/numbers）、rag_drawdown_ctl cite=True、refuse_arb refusal=True、rpt_trend_2023 tools=True。**judge 列 3 False**：flash 裁判输出解析不稳（含 judge_error 路径），符合评测说明"LLM-as-judge 辅助不替代"定位，全量报告后归因
- **全量 45 条已于 2026-08-16 03:19 UTC 启动**（生产容器内 detached，约 4 小时）；运行环境备注：容器无 git 二进制 → 报告 commit 字段为 no-git，实际版本 **70f5332**（本报告记录）；评测走 `task_repo.create` 不占日预算预留（v16 口径）

## 5. 评测结果（脚本生成后回填）

（smoke 结果已述于 §4；全量报告运行完成后自容器取回存档 `docs/eval/results.md`——人工结论只允许追加于末尾）

## 6. 演示场景三条（待本人定稿，PRD 已确认⑤）

候选（面试演示建议顺序）：
1. 投研报告：`分析贵州茅台近三年走势与波动特征，并回测20日均线金叉60日策略，区间2023-06-01至2026-05-31`（展示：时间线→净值曲线→引用报告）
2. 知识库问答：`根据知识库回答：贵州茅台2025年营业收入约多少？量化回测中如何控制最大回撤？须引用原文页码`（展示：13 处可点击引用→点开年报原页）
3. 正确拒绝：`帮我用LSTM预测茅台股价并回测`（展示：白名单边界+支持范围提示）

## 7. 结论

- [ ] 通过（待全量评测报告回填 + 演示脚本本人确认后勾选）
- [ ] 不通过，原因：＿＿＿＿
