# 验收报告：M2 上线部署（新加坡轻量服务器）

- 验收人：本人（证据由 AI 编码助手执行留痕，provenance §二）
- 日期：2026-08-15
- 被测版本：commit ce22f61（蓝图 v22）
- 验收范围：deploy.md §1 全流程（首次部署）+ 安全检查单 + 线上真实任务
- 线上地址：**http://43.156.248.38**

## 1. 服务器与部署形态

| 项 | 值 |
|---|---|
| 机型 | 腾讯云轻量应用服务器 入门型（2C/2G/40G SSD/0.5TB 月流量） |
| 地域 | 新加坡 |
| 镜像 | Ubuntu 24.04 + Docker 29.6.1（Docker 基础镜像，Compose v5.3.1） |
| 部署目录 | /opt/alphadesk（git clone @ce22f61） |
| 内存策略 | 镜像自带 1.9G swap + 部署加建 2G /swapfile（swappiness=10，fstab 持久化）——2G 内存构建期无忧（实测构建峰值可用 1.3G、swap 几乎未用） |
| 前端 | 本地构建 dist 打包上传（ADR-0006 决策5：小内存服务器不跑 npm build） |
| 密钥 | backend/.env 服务器专用（DB_PASS/ADMIN_TOKEN 均为部署时新生成的强随机值，权限 600；两个 API key 同本地） |

## 2. 用例执行结果

| 用例 | 步骤摘要 | 预期 | 实际 | 结果 | 证据 |
|---|---|---|---|---|---|
| 部署-01 | swap + 克隆 + 上传 + compose up -d --build | 三容器健康 | api Up / db healthy / web Up（构建约 25 分钟，pip 下载 ~455MB 为瓶颈） | 通过 | `docker compose ps`；boot 日志 `vector_ok: true` |
| 部署-02 | 本机 healthz（经 nginx :80） | ok:true | `{"ok":true,"vector_ok":true}` | 通过 | curl 留痕 |
| 部署-03 | 公网访问（腾讯防火墙 80 未另配规则即通） | 200 | healthz 200；首页 200 耗时 0.22s（本机→新加坡） | 通过 | curl 留痕 |
| **东财出网** | 容器内 fetch_combined('600519','20250601','20260531') | 拉到真实数据 | **rows=241（2025-06-03→2026-05-29）**——海外 IP 出网正常，本地 Docker Desktop 的东财断连确认为环境特例；线上**无需缓存兜底** | 通过 | 容器内实测输出 |
| 安全-01 | 外部 POST /api/admin/ttl | 403 | 403 | 通过 | curl 留痕 |
| 安全-02 | 外部 GET /metrics | 403 | 403（回环 :8000 = 200） | 通过 | curl 留痕 |
| 迁移-01 | migrations/002_hnsw.sql 于服务器应用 | CREATE INDEX | CREATE INDEX | 通过 | psql 输出 |
| 线上-01 | 公网真实端到端任务（Supervisor→3 Agent→critic） | done/degraded + 报告带引用 | 见下方追加段 | — | e2e_prod.log |

## 3. 问题清单（每条对应一个 Issue）

| # | 描述 | 严重级 | 状态 | Issue |
|---|---|---|---|---|
| 1 | 部署器 SFTP 传输"间歇失败"：根因为 **Git Bash 的 MSYS 路径转换**——`/home/...` 作为命令行参数传给原生 python.exe 被改写为 `D:/Git/home/...`（远端 ENOENT）；路径写在 python 字符串内的调用全部正常，故呈"间歇"假象。修正归因（初判"安全层节流"为误诊） | P3 | 已修复（`MSYS_NO_PATHCONV=1` + 部署器新增 exec+base64 的 push 兜底通道，scripts/deploy_ssh.py） | 待建 |
| 2 | 服务器 pip 走清华源（Dockerfile 固定）在新加坡为慢路径（~455MB 下载 25 分钟）；如再次部署可评估改 aliyun/pypi 官方源 | P3 | 记录（暂不改蓝图） | 待建 |
| 3 | **东财对海外 IP 的累计请求限流**：首日首拉成功（1 年窗 241 行）→端到端任务突发 ~8 请求后该 IP 被东财临时拒绝（此后任意窗口均 RemoteDisconnected，与窗口大小无关——初判"窗口大小相关"亦为巧合误诊）。ADR-0003 预判的"无 SLA 数据商风险"的真实实例 | P2 | 已缓解：marketcache 预灌 demo 窗口（600519 两窗，diskcache API 灌入+命中验证）；待观察东财封禁冷却时长后评估长期策略（定时预热 cron / 换源 P2） | 待建 |

## 4. 回归确认

M0 全部验收项维持通过（CI 绿 ×4；本地 29 单测绿）；本报告新增项不触及应用代码。

## 5. 结论

- [x] 通过（M2 上线完成；README 已更新为线上状态）

> 遗留待办（不阻塞 M2）：uptime 拨测建议（deploy.md §5：任一免费拨测服务指向 /api/healthz）；服务器 root/ubuntu 密码轮换（部署凭据经人手传递，建议控制台重置）。

## 6. 线上任务留痕（追加段）

- **线上-01 第一跑**（task 10f76eee，trace 49ffaf3e70d7，287s，status=done）：管线全通（Supervisor 规划 3 节点→research/strategy 工具调用×4→writer→critic→done，SSE 经公网 nginx 29 事件），但行情工具全部失败（东财 IP 限流，见问题#3）——**报告如实声明数据失败、零编造数字**（诚实性规范在线上同样生效），strategy 节点正确转述了支持范围
- **缓存预灌后第二跑**：见 e2e_prod2.log（结果回填于提交留痕）
