# M0 验收清单（验收人：本人；逐项留痕后归档为验收报告）

> 用法：逐项执行，勾选并填写"证据"列（命令输出/日志/trace_id 引用）。任何一项失败=验收不通过，开 GitHub Issue（本地阶段记录于本文件"问题区"），修复后重验。
> 被测版本：蓝图 v20 落仓代码 ＋ commit hash（git init 后填写）：**9a9b0b9**（v19 落仓提交；v20 端到端补验提交见 `git log`）
> 执行记录：2026-08-15，AI 编码助手按蓝图执行并留痕（provenance §二：过程监督与验收属本人）；完整失败原文见 `docs/verification/M0-记录.md` §5.1/§3.5。

## A. 单元测试（不需要 API key）✅

- [x] A1 `pytest tests/test_backtest.py` 全绿（四类断言：成交时序/手算含费/next_open/指标健全）
      证据：4 passed（修复 #5~#7 后，v19；失败原文见 M0-记录 §5.1）
- [x] A2 `pytest tests/test_dsl.py` 全绿（十类拒绝 + hhv/llv 不含当日 + exit 优先 + ret + RSI 横盘）
      证据：15 passed（修复 #8 后）
- [x] A3 `pytest tests/test_events_replay.py` 全绿（竞态无重无漏/重叠去重/终态关流/终态立即返回/溢出通知/心跳不终止流）
      证据：6 passed（一次通过，未改动）
- [x] A4 `pytest tests/test_rag_chunk.py` 全绿（不跨页/页归属/扫描页拒绝）
      证据：3 passed（修复 #9 后）；合计 **29 passed in 1.58s**（Py3.11 venv）

## B. AKShare 数据实证（不需要 API key）✅

- [x] B1 拉取 600519 双口径（hfq+raw）成功，中文列 schema 校验通过
      证据：`scripts/m0_akshare_checks.py` → 724 行 × 10 列，区间 2023-06-01~2026-05-29
- [x] B2 hfq 重叠窗口实证：2023-06-01~2024-06-30 与 2023-06-01~2026-05-31 重叠段逐日一致？
      结果：**一致**（261 重叠日 × 10 列最大绝对差全 0.0；ADR-0003 已回填）
      证据：M0-记录 §4 / ADR-0003 决策 2
- [x] B3 生成首份评测快照 `evals/fixtures/600519_hfq_20230601_20260531.parquet`（含 meta：fetched_at/akshare_version/checksum；
      **完整 10 列双口径帧**：date 索引 + OHLCV × hfq/raw——命名中 hfq 指计算口径非列范围，`tools._load_fixture` 将校验列齐备，v18 P2-1）
      证据：724 行 sha1=236554f43075…，meta sidecar `.meta.json`；另生成 breakout 用例快照（580 行）；回读校验 OK

## C. GLM 与 embedding 实测（**需要 API key**）✅（C2 阻塞待按量余额）

- [x] C1 真实 key 调用 glm-4.6 与 glm-4.7-flash 各一次，usage 字段正常
      证据：glm-4.6 `usage_tokens=189`（复跑 167）/ glm-4.7-flash `usage_tokens=154`（M0-记录 §3，脚本 m0_llm_checks.py）；**tools=None 一并闭环**（生产路径默认形态即成功）
- [ ] C2 embedding-3(dimensions=1024) 返回维度=1024；探针日志记录
      证据：**阻塞**——429 code 1113 余额不足（该 key 的 embedding 家族不在资源包内，见 §3 key 差异结论）；探针两种失败路径（无 key Connection error / 有 key 1113）均优雅降级不阻止启动已实证
- [x] C3 模拟维度异常路径：检索降级 BM25 且前端标注（可代码级演练）
      证据：`m0_drill_bm25_fallback.py` → bm25_degraded + 明示 note + M_RAG_FALLBACK 0→1；端到端任务中文口径标注已随报告呈现（§3.5）

## D. 运行时演练（不需要 API key 的部分）✅

- [x] D1 Compose 起三容器健康（db/api/web）；健康检查与依赖顺序生效
      证据：db `healthy`、api/web `Up`；healthz 直连与经 nginx 均通；启动日志 boot event（M0-记录 §5.2）
- [x] D2 guarded finish 冲突演练：人为使任务心跳停超租约→watchdog 标 interrupted→迟到的 finish 不覆盖（M_TASK_CONF+1）、预留被释放；
      扩展（v18 P1-1）：重启对账演练后断言 `usage_day.reserved = Σ(tasks.reserved WHERE status='pending')`（当日行）——防双重释放回归
      证据：`m0_drill_guarded_finish.py` 三场景全过（50000 释放/不覆盖+计数/37777→30000==Σpending）（M0-记录 §5.3）
- [x] D3 并发冒烟：两任务并行，事件循环不阻塞（to_thread 生效，心跳正常）
      证据：HTTP 面双任务并行期 healthz max 16ms；容器内 180 次并发 to_thread 网络IO 期事件循环 max_lag 2.2ms、心跳续租 59 次（M0-记录 §5.4）
- [x] D4 备份链演练：backup.sh 全链成功（flock/dump→tar→ttl）；reconcile 零悬空
      证据：等价链逐步执行（**偏差：Git Bash 无 flock，互斥留服务器首部署验证**）；db.sql.gz 完整性 OK；TTL {"deleted":0}；reconcile `{"dangling":[]}`；外部 admin/metrics 403、回环 metrics 200（M0-记录 §5.6）

## E. 环境一致性 ✅

- [x] E1 Python 3.11 venv 安装 requirements 全成功（wheel 兼容性实测）
      证据：Py3.11.0 全装成功（含 httpx 钉 0.27.0 复装；M0-记录 §5.7）
- [x] E2 前端 `npm ci && npm run build` 成功（tsc -b 无错）
      证据：`npm install` 78 包（无 lockfile 首装即 install 生成 package-lock.json，此后可 ci）+ `tsc -b && vite build` 0 错误，622 模块

## 问题区（验收中发现的问题逐条记录，格式：编号/描述/严重级/状态/关联 Issue）

| # | 描述 | 严重级 | 状态 | Issue |
|---|---|---|---|---|
| 1 | compose 顶键 volumes 缩进于 services 下→校验失败 | P1（部署阻断） | 已修复（v19，本地 Issue 待建） | 待建 |
| 2 | Dockerfile CMD JSON 数组跨行→parse error | P1（部署阻断） | 已修复（v19） | 待建 |
| 3 | pydantic-settings extra_forbidden 拒绝 .env 共享变量（本地测试全挂/容器启动即崩） | P0 | 已修复（v19） | 待建 |
| 4 | httpx~=0.27 解析 0.28.x→zhipuai import sniffio 崩溃 | P0 | 已修复（钉 0.27.0） | 待建 |
| 5 | test_fill_timing_contract signal_close 断言与契约矛盾 | P2（测试缺陷） | 已修复（v19） | 待建 |
| 6 | test_hand_computed_with_fee 漏计第二笔费 | P2（测试缺陷） | 已修复 | 待建 |
| 7 | test_max_drawdown 数据捕获段净额恰 0 | P2（测试缺陷） | 已修复 | 待建 |
| 8 | test_hhv_excludes_today 数据不足 MA20 窗口 | P2（测试缺陷） | 已修复 | 待建 |
| 9 | test_chunk_page_attribution 文本低于无文本层阈值 | P2（测试缺陷） | 已修复 | 待建 |
| 10 | 环境留档：容器出网东财被拒（宿主机正常）/Git Bash 无 flock/pandas read_json FutureWarning | 观察 | 不阻塞（记录在案，生产部署复测） | — |
| 11 | agent_loop `from app.tools import registry` 引用不存在对象——首个真实 Agent 节点即 ImportError（静态审查两轮漏检） | P0 | 已修复（v20） | 待建 |
| 12 | Supervisor 无日期锚点——"近三年"被解析为 2021-2024（训练截止时钟） | P1 | 已修复（v20，规划消息注入日期行） | 待建 |
| 13 | Dockerfile COPY 把含密钥的 .env 烤进镜像 | P1（安全） | 已修复（v20，backend/.dockerignore；重建后验证 /app/.env 不存在） | 待建 |
| 14 | key 余额：embedding 家族持续 1113、glm-4.6 间歇 1113（coding-plan 型 key） | 观察 | C2 阻塞；正式演示需按量付费/充值 | — |

## 验收结论

- [ ] 通过，进入 M1
- [ ] 有条件通过，条件：C2（embedding-3 实测维度）待按量付费余额后补验（key 差异结论已记录 §3）
- [ ] 不通过，原因：＿＿＿＿

验收人签名/日期：＿＿＿＿
